## 分析
第一种方法可以采用双指针，左右指针同时向中间遍历，就会出现下面三种情况：
- `(left == val)&&(right != val)` 题干要求所有val都在右边，所以可以直接swap函数替换
- `(left != val)&&(right == val)`这种情况不需要动任何东西，接着遍历
- `(left == val)&&(right == val)`这种情况需要左指针不动，右指针继续往左移动知道出现第一种情况，再swap替换
这样可以保证数组的输出是符合要求的；
对于k，可以
## 代码

---
## 分析2

## 代码

## 分析3

## 代码
