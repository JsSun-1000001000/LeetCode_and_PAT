## [33. 搜索旋转排序数组 - 力扣（LeetCode）](https://leetcode.cn/problems/search-in-rotated-sorted-array/description/)

## 代码
```cpp
class Solution {
public:
    int search(vector<int>& nums, int target) {
        int left = 0;
        int right = nums.size() - 1;
        while (left <= right) {
            int middle = (right + left) / 2;
            if (target == nums[middle]) {
                return middle;
            }
            else if (nums[middle] > target) {
                if (nums[left] < nums[middle]) {
                    if (target >= nums[left]) {
                        right = middle - 1;
                    }
                    else {
                        left = middle + 1;
                    }
                }
                else if (nums[left] > nums[middle]) {
                    right = middle - 1;
                }
                else if (nums[left] == nums[middle]) {
                    left = middle + 1;
                }
            }
            else if (nums[middle] < target) {
                if (nums[left] < nums[middle]) {
                    left = middle + 1;
                }
                else if (nums[left] > nums[middle]) {
                    if (nums[left] <= target) {
                        right = middle - 1;
                    }
                    else {
                        left = middle + 1;
                    }
                }
                else if (nums[left] == nums[middle]) {
                    left = middle + 1;
                }
            }
        }
        return -1;
    }
};
```

## 扩展：[面试题 10.03. 搜索旋转数组 - 力扣（LeetCode）](https://leetcode.cn/problems/search-rotate-array-lcci/description/)

## 代码
